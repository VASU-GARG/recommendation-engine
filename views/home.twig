<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    {% if c|length > 0 %}
    <h1>RECOMMENDED COURSES</h1>
        {% for x in c %}
            <h3> Because You watched {{x[0].courseName}} </h3>
            {% for y in 1..(x|length)-1 %}
                <form action="/newCourseSelect" method="POST">
                    <input type="submit" name="course" value="{{x[y].courseName}}">
                </form>
            {% endfor %}
        {% endfor %}
    {% endif %}


    <h1>ALL COURSES </h1>
        {% for x in all_courses %}
            <form action="/newCourseSelect" method="POST">
                <input type="submit" name="course" value="{{x.courseName}}">
            </form>
        {% endfor %}
</body>
</html> 